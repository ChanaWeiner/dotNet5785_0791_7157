<Window x:Class="PL.StudentCall.CallsHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.StudentCall"
        mc:Ignorable="d"
        Title="CallsHistoryWindow" Height="450" Width="800"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Content="Filter by End of Treatment:" VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
            <ComboBox Grid.Column="1" FontSize="16" Height="30" SelectedValue="{Binding EndType, Mode=TwoWay}" SelectionChanged="FilterClosedCalls" ItemsSource="{Binding Source={StaticResource EndOfTreatmentCollectionKey}}"/>
        </Grid>
        <DataGrid Grid.Row="1" ItemsSource="{Binding ClosedCallInLists}" AutoGenerateColumns="True" CanUserReorderColumns="False" CanUserResizeRows="False" CanUserSortColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionUnit="FullRow" Margin="10,10,10,10"/>
    </Grid>
</Window>
