<Window x:Class="PL.StudentCall.OpenCallsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.StudentCall"
                        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"

        mc:Ignorable="d"
        Title="OpenCallsWindow" Height="600" Width="900"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Search:" VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
            <ComboBox Width="200" FontSize="16" Height="30" SelectedValue="{Binding SelectedSearchOption}" ItemsSource="{Binding Source={StaticResource OpenCallsFieldsCollectionKey}}" />
            <TextBox Width="200" Height="30" FontSize="16" Text="{Binding SearchValue, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Search" Width="100" Height="30" FontSize="16" Margin="10,0,0,0" Click="SearchButton_Click"/>
            <Button Content="Clear" Width="100" Height="30" FontSize="16" Margin="10,0,0,0" Click="ClearButton_Click"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Sort:" VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
            <ComboBox Width="200" FontSize="16" Height="30" SelectedValue="{Binding SelectedSortOption}" ItemsSource="{Binding Source={StaticResource OpenCallsFieldsCollectionKey}}" />

            <Button Content="Sort" Width="100" Height="30" FontSize="16" Margin="10,0,0,0" Click="SortButton_Click"/>
            <Button Content="Reset Sort" Width="100" Height="30" FontSize="16" Margin="10,0,0,0" Click="ClearButton_Click"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid 
                  Grid.Row="2" 
                  ItemsSource="{Binding OpenCalls}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionChanged="OpenCallsDataGrid_SelectionChanged"
                  SelectedItem="{Binding SelectedCall}"
                  Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Subject}"/>
                <DataGridTextColumn Header="Address" Binding="{Binding FullAddress}"/>
                <DataGridTextColumn Header="Distance" Binding="{Binding DistanceFromTutor}"/>
                <DataGridTemplateColumn Header="Assign">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Assign" Click="AssignCall_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <ScrollViewer>

        </ScrollViewer>
        <StackPanel Grid.Row="3" Margin="10">
            <TextBlock Text="Call Description:" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Height="100" Background="#f5f5f5" Padding="10"/>
        </StackPanel>
        <wv2:WebView2  Grid.Row="4"  x:Name="MyWebView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />

    </Grid>
</Window>
