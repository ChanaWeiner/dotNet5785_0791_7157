<Window x:Class="PL.Tutor.TutorListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Tutor"
        mc:Ignorable="d"
        Title="Tutor List"
        Width="800"
        Height="640"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        Background="White">

    <Window.Resources>
        <DropShadowEffect x:Key="DropShadow"
                          BlurRadius="6"
                          ShadowDepth="2"
                          Opacity="0.3"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Search:" VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
            <ComboBox Width="200" FontSize="16" Height="30" SelectedValue="{Binding SelectedSearchOption}" ItemsSource="{Binding Source={StaticResource TutorFieldsCollectionKey}}" />
            <TextBox Width="200" Height="30" FontSize="16" Text="{Binding SearchValue, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Search" Width="100" Height="30" FontSize="16" Margin="10,0,0,0" Click="SearchButton_Click"/>
            <Button Content="Clear" Width="100" Height="30" FontSize="16" Margin="10,0,0,0" Click="ClearButton_Click"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Sort:" VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0"/>
            <ComboBox Width="200" FontSize="16" Height="30" SelectedValue="{Binding SelectedSortOption}" ItemsSource="{Binding Source={StaticResource TutorFieldsCollectionKey}}" />

            <Button Content="Sort" Width="100" Height="30" FontSize="16" Margin="10,0,0,0" Click="SortButton_Click"/>
            <Button Content="Reset Sort" Width="100" Height="30" FontSize="16" Margin="10,0,0,0" Click="ClearButton_Click"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding TutorsList}"
                  SelectedItem="{Binding SelectedTutor}"
                  MouseDoubleClick="LsvTutorsList_MouseDoubleClickHandler"
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  FontSize="16"
                  Margin="0"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"/>

        <!-- Add Button with style -->
        <Button Grid.Row="3" Content="Add" Click="ButtonAdd_Click"/>
    </Grid>
</Window>
